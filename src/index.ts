import {Namespace, PrintNamespace, withNamespace, withNamespaceClass} from "./namespaces";
import {useNamespace} from "./useNamespace";

export default Namespace
export {Namespace, PrintNamespace, useNamespace, withNamespace, withNamespaceClass}
